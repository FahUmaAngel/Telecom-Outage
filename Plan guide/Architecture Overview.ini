┌──────────────┐
│   Frontend   │  (React / Next.js)
│  (Web App)   │
└──────┬───────┘
       │ HTTPS (REST / GraphQL)
┌──────▼────────┐
│   Backend API │  (FastAPI / Node.js)
│  (Auth + Data)│
└──────┬────────┘
       │
┌──────▼───────────────┐
│  Application Layer   │
│  (Outage Engine)     │
└──────┬───────────────┘
       │
┌──────▼───────────────┐
│   Database Layer     │
│ (Postgres + PostGIS) │
└──────┬───────────────┘
       │
┌──────▼───────────────┐
│  Data Ingestion      │
│ (Scrapers / Workers) │
└──────────────────────┘
